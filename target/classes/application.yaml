#spring.application.name=spring-eshop
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/shop
    username: postgres
    password: DeMBli1997
  flyway:
    baseline-on-migrate: true
